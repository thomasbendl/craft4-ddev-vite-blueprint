{% extends "_layout" %}

{% block content %}
<div class="container" data-barba="container" data-barba-namespace="home">
        <div class="centerBox">
                <div class="animate">
                        <h1>{{ craft.includeWeather() }}</h1>
                        <p><a href="/ready">next</a></p>
                </div>
        </div>        
</div>

{% endblock %}
